---
phase: 05-devrev-webhook-reply-loop
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - devrev-web/.circleci/config.yml
  - devrev-web/.circleci/nitpick-agent.yml
  - .github/workflows/deploy-snap-in.yml
  - src/service-worker/messages.ts
autonomous: true
requirements: [DREV-02, DREV-05]

must_haves:
  truths:
    - "CircleCI pipeline accepts nitpick_mode parameter alongside nitpick_issue_id"
    - "Snap-in deploys automatically via GitHub Actions on PR merge"
    - "Chrome extension issues end with 'Made with Nitpick' marker"
    - "CircleCI nitpick-agent job passes both nitpick_issue_id and nitpick_mode to the shell runner"
  artifacts:
    - path: "devrev-web/.circleci/nitpick-agent.yml"
      provides: "CircleCI continuation config with nitpick_mode parameter"
      contains: "nitpick_mode"
    - path: "devrev-web/.circleci/config.yml"
      provides: "Setup config routing with nitpick_mode parameter declaration"
      contains: "nitpick_mode"
    - path: ".github/workflows/deploy-snap-in.yml"
      provides: "GitHub Actions workflow for snap-in marketplace deployment"
      contains: "create-marketplace-submission"
    - path: "src/service-worker/messages.ts"
      provides: "Chrome extension issue body with 'Made with Nitpick' marker"
      contains: "Made with"
  key_links:
    - from: "devrev-web/.circleci/config.yml"
      to: "devrev-web/.circleci/nitpick-agent.yml"
      via: "continuation/continue with params"
      pattern: "nitpick-agent.yml"
    - from: "devrev-web/.circleci/nitpick-agent.yml"
      to: "devrev-web/scripts/circleci/run_nitpick_fix_agent.sh"
      via: "bash invocation step"
      pattern: "run_nitpick_fix_agent.sh"
    - from: ".github/workflows/deploy-snap-in.yml"
      to: "snap-in/"
      via: "marketplace submission on PR merge"
      pattern: "create-marketplace-submission"
---

<objective>
Wire the CircleCI pipeline to accept the new nitpick_mode parameter, add the GitHub Actions deploy workflow for the snap-in, and update the Chrome extension to append the "Made with Nitpick" marker.

Purpose: Plan 01 (snap-in) and Plan 02 (runner/prompt) are the logic -- this plan wires them into the infrastructure: CI pipeline config, automated deployment, and extension marker.

Output: Updated CircleCI config with nitpick_mode support, GitHub Actions workflow for snap-in deploy, and Chrome extension marker change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-devrev-webhook-reply-loop/05-CONTEXT.md
@.planning/phases/05-devrev-webhook-reply-loop/05-RESEARCH.md
@.planning/phases/05-devrev-webhook-reply-loop/05-PATTERNS.md
@.planning/phases/04-foundation-agent-core/04-03-SUMMARY.md

<interfaces>
<!-- Phase 4 established the CircleCI config pattern (04-03-SUMMARY): -->
<!-- - .circleci/config.yml has `nitpick_issue_id` pipeline parameter -->
<!-- - .circleci/config.yml has routing block matching scheduled_job_name=nitpick_fix_agent -->
<!-- - .circleci/nitpick-agent.yml is the continuation config -->
<!-- Phase 5 adds: nitpick_mode parameter alongside nitpick_issue_id -->

From 04-03-SUMMARY (config.yml additions):
  - Pipeline parameter: nitpick_issue_id (type: string, default: '')
  - Routing: jq creates params JSON, continuation/continue -> nitpick-agent.yml

From design-system deploy workflow:
  uses: devrev/github-actions/create-marketplace-submission@create-marketplace-submission-v0.1.12
  with:
    code_ref_url: ${{ github.event.pull_request.url }}
    github_token: ${{ secrets.GITHUB_TOKEN }}
    read_actions_token: ${{ secrets.READ_GITHUB_ACTIONS_RELEASES }}
  env:
    DEVREV_PAT: ${{ secrets.DEVREV_DEV_PAT }}

From extension messages.ts (issue creation):
  Lines 143-195 contain the issue body assembly with markdown images and metadata.
  The body is assembled as a string with sections (Screenshots, Description, Code identifiers).
  Append "Made with [Nitpick](https://github.com/xorkavi/nitpick)" at the end.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CircleCI config update (add nitpick_mode parameter)</name>
  <files>
    devrev-web/.circleci/config.yml
    devrev-web/.circleci/nitpick-agent.yml
  </files>
  <read_first>
    /Users/kavinash/arcade/devrev-web/.circleci/config.yml
    /Users/kavinash/arcade/devrev-web/.circleci/nitpick-agent.yml
  </read_first>
  <action>
Modify the existing CircleCI configuration to support the `nitpick_mode` parameter. Phase 4 already added `nitpick_issue_id` -- we add `nitpick_mode` alongside it.

**In `.circleci/config.yml`:**

1. Add new pipeline parameter under the existing `parameters:` section:
```yaml
  nitpick_mode:
    type: string
    default: "analysis"
```

2. In the nitpick routing block (the `jq` command that creates params JSON), add `nitpick_mode` to the params JSON:
```bash
jq -n --arg issue_id "<< pipeline.parameters.nitpick_issue_id >>" --arg mode "<< pipeline.parameters.nitpick_mode >>" '{nitpick_issue_id: $issue_id, nitpick_mode: $mode}' > /tmp/nitpick-agent-params.json
```

**In `.circleci/nitpick-agent.yml`:**

1. Add `nitpick_mode` to the `parameters:` section:
```yaml
parameters:
  nitpick_issue_id:
    type: string
    default: ""
  nitpick_mode:
    type: string
    default: "analysis"
```

2. In the job environment, export `NITPICK_MODE`:
```yaml
environment:
  NITPICK_ISSUE_ID: << pipeline.parameters.nitpick_issue_id >>
  NITPICK_MODE: << pipeline.parameters.nitpick_mode >>
```

3. Remove the `query_nitpick_issues.sh` step (D-03: snap-in replaces cron entirely). The issue ID is now always passed as a parameter by the snap-in. Remove the conditional logic that called query_nitpick_issues.sh.

4. Ensure the agent runner step uses `NITPICK_MODE`:
```yaml
- run:
    name: Run nitpick fix agent
    command: bash scripts/circleci/run_nitpick_fix_agent.sh
    no_output_timeout: 30m
```

5. Remove cron schedule workflow (D-03: no cron fallback). Keep only the pipeline-triggered workflow. The workflow should have a `when` condition that checks if `nitpick_issue_id` is not empty:
```yaml
workflows:
  nitpick_fix:
    when:
      not:
        equal: ["", << pipeline.parameters.nitpick_issue_id >>]
    jobs:
      - nitpick_fix_agent:
          context:
            - github
            - aws-dev-account
            - npm
            - devrev-api
```
  </action>
  <verify>
    <automated>cd /Users/kavinash/arcade/devrev-web && grep -q "nitpick_mode" .circleci/config.yml && grep -q "nitpick_mode" .circleci/nitpick-agent.yml && grep -q "NITPICK_MODE" .circleci/nitpick-agent.yml && python3 -c "import yaml; yaml.safe_load(open('.circleci/nitpick-agent.yml'))" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .circleci/config.yml declares `nitpick_mode` pipeline parameter (type: string, default: "analysis")
    - .circleci/config.yml nitpick routing block passes both `nitpick_issue_id` and `nitpick_mode` to continuation
    - .circleci/nitpick-agent.yml declares `nitpick_mode` parameter
    - .circleci/nitpick-agent.yml job environment exports `NITPICK_MODE: << pipeline.parameters.nitpick_mode >>`
    - .circleci/nitpick-agent.yml does NOT reference `query_nitpick_issues.sh` (removed per D-03)
    - .circleci/nitpick-agent.yml passes YAML validation (python3 yaml.safe_load)
    - .circleci/nitpick-agent.yml job runs `bash scripts/circleci/run_nitpick_fix_agent.sh` with no_output_timeout: 30m
    - No cron schedule workflow present for nitpick (D-03: snap-in replaces cron)
  </acceptance_criteria>
  <done>CircleCI config accepts nitpick_mode parameter from snap-in trigger, passes it through continuation to the agent job, and the cron fallback is removed.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions deploy workflow + Chrome extension marker</name>
  <files>
    .github/workflows/deploy-snap-in.yml
    src/service-worker/messages.ts
  </files>
  <read_first>
    /Users/kavinash/arcade/plugins/nitpick/src/service-worker/messages.ts
    .planning/phases/05-devrev-webhook-reply-loop/05-PATTERNS.md
  </read_first>
  <action>
**1. Create `.github/workflows/deploy-snap-in.yml`** (per D-04, following design-system pattern):

```yaml
name: Deploy Nitpick Snap-in

on:
  pull_request:
    types: [closed]
    paths:
      - 'snap-in/**'

jobs:
  create-marketplace-submission:
    name: Create marketplace submission
    if: github.event.pull_request.merged
    runs-on: ubuntu-latest
    steps:
      - name: Create marketplace submission
        uses: devrev/github-actions/create-marketplace-submission@create-marketplace-submission-v0.1.12
        with:
          code_ref_url: ${{ github.event.pull_request.url }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          read_actions_token: ${{ secrets.READ_GITHUB_ACTIONS_RELEASES }}
        env:
          DEVREV_PAT: ${{ secrets.DEVREV_DEV_PAT }}
```

Key differences from the design-system version:
- Added `paths: ['snap-in/**']` filter so it only triggers on snap-in changes
- Same action version and parameters

**2. Update `src/service-worker/messages.ts`** (per D-07):

Find the section where the issue body is assembled (around lines 143-195 based on CONTEXT.md reference). The body is built as a string with sections for screenshots, description, and code identifiers.

Append the following line at the very end of the issue body string, separated by two newlines:

```typescript
// At the end of the body assembly, before the final return/assignment:
body += "\n\n---\nMade with [Nitpick](https://github.com/xorkavi/nitpick)";
```

The exact implementation depends on how the body is currently assembled. Look for the final body string concatenation or template literal. The marker must appear as the very last content in the issue body. This serves as both attribution and an additional validation signal for the snap-in (though it is NOT a required validation criterion per Pitfall 6 in RESEARCH.md).
  </action>
  <verify>
    <automated>cd /Users/kavinash/arcade/plugins/nitpick && test -f .github/workflows/deploy-snap-in.yml && grep -q "create-marketplace-submission" .github/workflows/deploy-snap-in.yml && grep -q "snap-in/\*\*" .github/workflows/deploy-snap-in.yml && grep -q "Made with" src/service-worker/messages.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy-snap-in.yml exists with trigger `on: pull_request: types: [closed]`
    - .github/workflows/deploy-snap-in.yml has `paths: ['snap-in/**']` filter
    - .github/workflows/deploy-snap-in.yml uses `devrev/github-actions/create-marketplace-submission@create-marketplace-submission-v0.1.12`
    - .github/workflows/deploy-snap-in.yml has `if: github.event.pull_request.merged` condition
    - .github/workflows/deploy-snap-in.yml references secrets: GITHUB_TOKEN, READ_GITHUB_ACTIONS_RELEASES, DEVREV_DEV_PAT
    - src/service-worker/messages.ts contains string "Made with" followed by "Nitpick" (D-07 marker)
    - The marker is appended at the end of the issue body (last content before any closing)
  </acceptance_criteria>
  <done>GitHub Actions workflow deploys snap-in to DevRev marketplace on PR merge to snap-in/ path. Chrome extension appends "Made with Nitpick" marker to all new issues per D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> DevRev marketplace | Workflow deploys code to production snap-in |
| CircleCI API caller (snap-in) -> CircleCI pipeline | Pipeline parameters passed from external trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Spoofing | CircleCI pipeline trigger | mitigate | CircleCI API requires project-level token (stored in DevRev keyring); only the snap-in has access |
| T-05-12 | Tampering | Pipeline parameters | mitigate | `nitpick_mode` constrained to "analysis"/"fix"/"revision" by shell runner case statement; invalid values default to "analysis" |
| T-05-13 | Information Disclosure | GitHub Actions secrets | accept | Standard GitHub Actions secret management; DEVREV_DEV_PAT scoped to dev org only |
| T-05-14 | Elevation of Privilege | Snap-in deployment | mitigate | Only triggers on merged PRs to snap-in/ path; requires PR review before merge |
</threat_model>

<verification>
1. CircleCI config validates as YAML
2. nitpick_mode parameter declared and passed through continuation
3. Deploy workflow triggers only on snap-in/ path changes
4. Chrome extension marker present in issue body
5. No cron schedule remains (snap-in is sole trigger)
</verification>

<success_criteria>
- CircleCI pipeline accepts nitpick_mode from snap-in trigger (DREV-02 infrastructure)
- Snap-in auto-deploys on PR merge (D-04)
- Chrome extension marks issues for snap-in validation (D-07)
- Cron removed entirely (D-03)
- Pipeline correctly routes both parameters to shell runner
</success_criteria>

<output>
After completion, create `.planning/phases/05-devrev-webhook-reply-loop/05-03-SUMMARY.md`
</output>
